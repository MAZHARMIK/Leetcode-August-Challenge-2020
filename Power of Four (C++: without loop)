class Solution {
public:
    bool isPowerOfFour(int num) {
        if(num <= 0)
            return false;
        double x = floor(log(num)/log(4));
        double y = ceil(log(num)/log(4));
        
        return x==y;
    }
};
