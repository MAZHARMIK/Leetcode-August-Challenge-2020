class Solution {
public:
    int rand10() {
        int row = rand7();
        int col = rand7();
        int idx = (row-1)*7+col;
        while(idx>40) {
            row = rand7();
            col = rand7();
            idx = (row-1)*7+col;
        }
        return (idx%10)+1;
    }
};
